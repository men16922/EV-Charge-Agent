Drop the generated EV-Charge Agent avatar here as: ev-agent.png
(square, ~256x256, transparent or solid background). The chat header loads /static/ev-agent.png
and silently hides it if the file is absent.
